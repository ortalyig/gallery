var number = 1;var slides = document.getElementsByClassName("photo-slide"); //מערך דיבים var bottomImages = document.getElementsByClassName("bottom-img"); // מערך תמונות var imageText = document.getElementById("image-text"); // כותרת תמונהfunction moveImgae(index) {    number += index;    imageShow(number);}function selectedImage(index) {    number = index;    imageShow(number);}function checkNumber(index) {    if (index > slides.length) {        number = 1;        return number;    }    else if (index < 1) {        number = slides.length;        return number = slides.length;    }    else {        return number;    }}function imageShow(index) {    number = checkNumber(index);    for (var i = 0; i < slides.length; i++) {        slides[i].style.display = "none";    }    for (var i = 0; i < bottomImages.length; i++) {        bottomImages[i].className = bottomImages[i].className.replace(" active", "");    }    slides[number - 1].style.display = "block";    bottomImages[number - 1].className += " active";    imageText.innerHTML = bottomImages[number - 1].alt;}